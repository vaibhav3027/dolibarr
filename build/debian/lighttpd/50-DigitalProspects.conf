# Alias for DigitalProspects directory
alias.url += ( 
	"/DigitalProspects" => "/usr/share/DigitalProspects/htdocs",
)

# Disallow access to libraries
#$HTTP["url"] =~ "^/DigitalProspects/libraries" { 
#    url.access-deny = ( "" ) 
#}

# Limit access to setup script
#$HTTP["url"] =~ "^/DigitalProspects/setup" {
#	auth.backend = "htpasswd"
#	auth.backend.htpasswd.userfile = "/etc/DigitalProspects/htpasswd.setup"
#	auth.require = (
#		"/" => (
#			"method" => "basic",
#			"realm" => "DigitalProspects Setup",
#			"require" => "valid-user"
#		)
#	)
#}
